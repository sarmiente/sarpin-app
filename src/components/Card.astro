---
interface Props {
  title: string;
  description: string;
  date: Date;
  image: string;
  href: string;
  category?: string;
    author?: string;
}

import { Image } from 'astro:assets';

const { title, description, date, image, href, category, author } = Astro.props;
---

<li class="bg-white rounded-lg overflow-hidden transition-shadow duration-300">
  <a href={href} class=" h-full">
     <Image width={400} height={256} class="object-cover object-center w-full h-64 rounded-lg" src={image} alt="" />

                <div class="">
                    <span class="text-blue-500 uppercase font-heading">{category}</span>

                    <h1 class="text-xl font-semibold font-heading leading-tight text-gray-900 dark:text-white hover:underline">
                             {title}
                    </h1>

                    <p class="mt-2 text-gray-500 dark:text-gray-400 text-sm font-body line-clamp-3">
                             {description}
                    </p>

                    <div class="justify-start mt-4">
                        <div class="font-heading leading-ti">
                            <a href="#" class="font-medium text-gray-700 dark:text-gray-300 hover:underline hover:text-gray-500">
                                {author}
                            </a>

                            <p class=" text-gray-500 dark:text-gray-400">
                                 {date.toLocaleDateString()}    
                            </p>
                        </div>

                        <a href="#" class="inline-block text-blue-500 underline hover:text-blue-400 text-sm">Leer más</a>
                    </div>

                </div>
  </a>
</li>


